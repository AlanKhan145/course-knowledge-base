# Bài tập 03 - Phân tích kết quả như một researcher

Dùng `data/table2_main_results.csv` và `data/table3_ablation.csv`.

1. Tìm metric nào StockMixer không đứng đầu trên NYSE.
2. So StockMixer với STHAN-SR trên NASDAQ cho bốn metric.
3. Tính phần trăm giảm RIC khi bỏ Time Mixing trên NASDAQ.
4. So `LSTM` và `LSTM + Stock Mixing`. Điều gì cho thấy Stock Mixing có giá trị độc lập?
5. Viết 5 câu nhận xét tách rõ:
   - fact từ bảng;
   - interpretation của paper;
   - giả thuyết riêng của bạn.
6. Kiểm tra giá trị NYSE SR của RSR-I trong CSV và hình bảng gốc. Vì sao không nên tự “sửa” dữ liệu chỉ vì thấy nó bất thường?

# Bài tập 01 - Tensor Shape Drill

Cho `N=500`, `T=16`, `F=10`, scales `{1,2,4}`.

1. Shape của input toàn thị trường là gì?
2. Shape của một stock trước và sau transpose trong Indicator Mixing?
3. Mỗi scale tạo sequence length bao nhiêu?
4. Nếu representation cuối chỉ concatenate theo time lengths như paper, `d` bằng bao nhiêu?
5. Shape của `H` trước Stock Mixing?
6. Nếu `m=12`, shape của `M1` và `M2` là gì?
7. Sau Stock Mixing, `H_hat` phải có shape nào để residual với `H` hợp lệ?

**Mục tiêu:** không code model trước khi trả lời được tất cả shape bằng tay.

# 012 - Using MongoDB to Store Sessions

## Section
Sessions & Cookies

## Duration
6min

## Main Idea
This lesson focuses on **Using MongoDB to Store Sessions** inside the **Sessions & Cookies** section. It is a study note generated from the provided course syllabus, so use it as a structured learning guide while you watch, code, or review the material.

## Learning Objectives
* Implement the core idea behind Using MongoDB to Store Sessions in the sample application.
* Trace how data moves through routes, controllers, models, views, or clients.
* Keep the code readable while adding behavior incrementally.

## Key Points
* MongoDB stores JSON-like documents, which changes how relationships and reads are modeled.
* Mongoose adds schemas and model methods on top of MongoDB collections.
* Document design should follow the way the application reads and writes data.
* Cookies are stored by the browser and sent back with matching requests.
* Sessions keep server-side state connected to a client through a session identifier.

## Practice
Recreate the change in a small branch or scratch file, then explain the request flow aloud from entry point to final response.

## Review Questions
* What problem does **Using MongoDB to Store Sessions** solve in this section?
* Which file, route, model, view, or client behavior would you inspect first?
* How would you prove that the final behavior works?

## Summary
The goal of this lesson is to make **Using MongoDB to Store Sessions** practical, not just familiar. After reviewing it, you should be able to explain how it fits into Sessions & Cookies and apply the idea in a small Node.js project.
